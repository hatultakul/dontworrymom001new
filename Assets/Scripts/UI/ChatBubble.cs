// Logic fully absorbed into PhoneUIController (UI Toolkit rewrite).
// BubbleSide enum kept here so any external references still compile.
namespace DontWorryMom.UI
{
    public enum BubbleSide { Mom, Player }
}
